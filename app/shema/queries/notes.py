from typing import AnyStr, Dict, List, Tuple, Union

from app.shema.models import Notes

def get_notes(note_id: Union[int, Tuple[int], List[int]] = None) -> Union[List[Dict[str, Union[int, str]]], Dict[str, Union[int, str]]]:
    pass
